task :check do
  system("bundle exec rspec --require ./chess.rb --fail-fast --colour specs/spec.rb")
  system("bundle exec rspec --require ./chess.rb --fail-fast --colour specs/checkmate_spec.rb")
end